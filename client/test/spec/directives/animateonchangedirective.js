'use strict';

describe('Directive: animateOnChangeDirective', function () {

  // load the directive's module
  beforeEach(module('serbleApp'));

  var
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
